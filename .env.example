NODE_ENV=production
PORT=3000
DATABASE_URL=postgres://bulkorder:CHANGE_ME@db:5432/bulkorder
REDIS_URL=redis://redis:6379

# Optional: when set, every valid spreadsheet row is POSTed to this HTTPS endpoint.
# The receiving order engine should honor the X-Idempotency-Key header.
DOWNSTREAM_ORDER_API_URL=
DOWNSTREAM_ORDER_API_TOKEN=
